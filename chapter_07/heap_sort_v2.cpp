#include <iostream>
#include <vector>

template <typename T> void build_heap(std::vector<T>& v);

template <typename T>
void heap_sort(std::vector<T>& v) {
    build_heap(v); // 将数组建堆
    
}

template <typename T>
void build_heap(std::vector<T>& v) {
    for ()
}